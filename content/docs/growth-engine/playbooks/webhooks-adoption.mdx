---
title: "Webhooks Adoption"
description: "Bot completed but no webhooks/callbacks configured"
icon: Mail
---

# Webhooks Adoption

<Callout type="info">
  **Priority:** 8 | **Emails:** 2 | **Cooldown:** 7 days
</Callout>

> Bot completed but no webhooks/callbacks configured

## Sequence Diagram

<Mermaid chart={`sequenceDiagram
    participant U as User
    participant S as System
    participant E as Email

    Note over S: Trigger: Bot completed but no webhooks/callbacks configured

    Note over U: 4 hours
    S->>E: Send webhooks-adoption
    E-->>U: Email delivered

    Note over U: +3 days
    S->>E: Send webhooks-adoption-followup
    E-->>U: Email delivered

    alt Stop Condition Met
        U->>S: Webhook or callback configured
    else
        U->>S: User replies to email
        Note over S: Playbook stops
    end`} />

## Email Steps

| Step | Delay | Template |
|------|-------|----------|
| d0 | 4 hours | `webhooks-adoption` |
| d3 | 3 days | `webhooks-adoption-followup` |

## Stop Conditions

- Webhook or callback configured
- User replies to email

---

## Email Templates

### 1. webhooks-adoption

**Sent:** 4 hours after trigger

<Callout type="info">
**Email Preview**

**Hi \{\{firstName\}\},**

You're polling for status. Webhooks are more scalable — event-driven architecture with zero polling overhead.

Why Webhooks

bot.completed payload

> **Pro Tip:** Pass custom metadata via

**[Configure Webhooks]** *(button)*

```
{ "event": "bot.completed", "data": { "bot_id": "...", "video_url": "...", "transcript": "...", "extra": {} } }
```
</Callout>

### 2. webhooks-adoption-followup

**Sent:** 3 days after previous email

<Callout type="info">
**Email Preview**

**Hi \{\{firstName\}\},**

Quick follow-up on webhooks. Here's a minimal handler to get you started.

Express.js handler

Webhook Payload Includes

> **Pro Tip:** Webhook URLs expire in 4h. Download artifacts immediately or call

**[Webhook Documentation]** *(button)*

```
app.post('/webhook', (req, res) => { if (req.body.event === 'bot.completed') { const { transcript, video_url } = req.body.data } res.sendStatus(200) })
```
</Callout>

