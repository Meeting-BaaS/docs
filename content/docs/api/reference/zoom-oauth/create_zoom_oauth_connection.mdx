---
title: Create Zoom OAuth Connection
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Exchanges a Zoom OAuth authorization code for access and refresh
          tokens, retrieves the Zoom user's profile, and stores the connection
          for managed OBF token generation. The authorization code is obtained
          by directing a Zoom user through the OAuth consent flow for your Zoom
          OAuth app. Once stored, you can reference this connection's
          `zoom_user_id` as the `zoom_obf_token_user_id` parameter when creating
          a bot, and the system will automatically fetch a fresh OBF token at
          join time. Note: the authorization code is single-use and expires in
          approximately 10 minutes.
      - content: >-
          authorization_code: The OAuth authorization code received from Zoom
          after user consent. This is a single-use code that expires in
          approximately 10 minutes.
      - content: >-
          redirect_uri: The redirect URI that was used in the OAuth
          authorization request. Must match exactly what was configured in your
          Zoom OAuth app.
      - content: >-
          zoom_client_id: Your Zoom OAuth app's Client ID, found in the Zoom App
          Marketplace under your app's credentials.
      - content: >-
          zoom_client_secret: Your Zoom OAuth app's Client Secret, found in the
          Zoom App Marketplace under your app's credentials.
      - content: >-
          connection_failure_data: Details about why the connection failed. Only
          present when `state` is `refresh_failed`.
      - content: 'created_at: Timestamp when the connection was first created.'
      - content: >-
          scopes: OAuth scopes granted by the user during authorization (e.g.
          `user:read:user`).
      - content: >-
          state: Connection state. `connected` means tokens are valid;
          `refresh_failed` means the token refresh failed and the user needs to
          re-authorize.
      - content: >-
          updated_at: Timestamp when the connection was last updated (e.g. token
          refresh).
      - content: 'uuid: Unique identifier for this connection.'
      - content: >-
          zoom_account_id: The Zoom account ID that the connected user belongs
          to.
      - content: >-
          zoom_user_id: The Zoom user ID of the connected user. Use this value
          as `zoom_obf_token_user_id` when creating a bot to have the system
          automatically fetch an OBF token for this user.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Exchanges a Zoom OAuth authorization code for access and refresh tokens, retrieves the Zoom user's profile, and stores the connection for managed OBF token generation. The authorization code is obtained by directing a Zoom user through the OAuth consent flow for your Zoom OAuth app. Once stored, you can reference this connection's `zoom_user_id` as the `zoom_obf_token_user_id` parameter when creating a bot, and the system will automatically fetch a fresh OBF token at join time. Note: the authorization code is single-use and expires in approximately 10 minutes.

<APIPage document={"./openapi.json"} operations={[{"path":"/zoom_oauth_connections/","method":"post"}]} />
